# speech_inpainting_project
This project is a Speech Inpainting System that reconstructs missing or corrupted audio samples. It uses a hybrid algorithm combining Spectral Sparse Coding and Gradient Regularization to mathematically guess missing data. Experiments evaluate its reconstruction quality (via SNR metrics) against random data loss and harsh AWGN noise injection.
